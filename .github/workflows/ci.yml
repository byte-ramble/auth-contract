name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  forge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Setup Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Install dependencies
        run: npm ci

      - name: Format Check
        run: forge fmt --check

      - name: Build
        run: forge build

      - name: Deploy Script Tests
        run: npm run test:deploy-scripts

      - name: Test
        run: forge test --offline -vv

      - name: Coverage
        run: forge coverage --offline --ir-minimum --report summary
